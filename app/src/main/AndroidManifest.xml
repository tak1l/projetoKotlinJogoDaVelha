<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fapce.caio.jogodavelha">
    <!-- Leitura -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" /> <!-- Escrita -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- Permissão para acessar a internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Permissão para verificar se tem uma conexão disponível -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.fapce.caio.jogodavelha.PaymentActivity" />
        <activity android:name="com.fapce.caio.jogodavelha.PlayersActivity" />
        <activity android:name="com.fapce.caio.jogodavelha.GameActivity" />
        <activity android:name="com.fapce.caio.jogodavelha.FileActivity" />
        <activity android:name="com.fapce.caio.jogodavelha.SqlActivity" />
        <activity android:name="com.fapce.caio.jogodavelha.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>